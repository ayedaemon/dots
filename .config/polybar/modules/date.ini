[module/date]
type = internal/date
interval = 1

time = "%H:%M"
time-alt = "%a, %b %d %H:%M:%S"

label = %time%


format = %{A3:alacritty --title calcurse --command calcurse:} <label>%{A3}
format-foreground = ${colors.fg}
format-padding = 1
